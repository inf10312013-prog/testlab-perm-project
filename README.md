# testlab-perm-project

This repo contains the project under:

- `testlab-perm-project/`

Please read: `testlab-perm-project/README.md`

## Highlights
- Linux permissions model for shared log/report directories
- setgid + default ACL to keep group-writable outputs stable
- demo scripts + systemd service/timer templates
- sample outputs included under `testlab-perm-project/docs/sample-output/`
